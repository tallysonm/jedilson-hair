{
  "buildCommand": "pnpm install && OUTDIR=$(pwd)/dist && pnpm --filter @workspace/barbershop exec vite build --config vite.config.ts --outDir $OUTDIR --emptyOutDir",
  "outputDirectory": "dist",
  "installCommand": "echo 'install runs in buildCommand'",
  "framework": null,
  "functions": {
    "api/[...slug].js": {
      "includeFiles": "api/app.handler.mjs"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/[...slug]" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
